<Page
    x:Class="ExpenseManager.App.Views.RecurringTransactionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:entities="using:ExpenseManager.Core.Entities">

    <Grid Padding="32,24,32,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Spacing="2">
                <TextBlock Text="Recurring" Style="{StaticResource AppPageTitleStyle}" />
                <TextBlock Text="Bills, subscriptions, and income that repeat on a schedule" Style="{StaticResource AppMutedTextStyle}" />
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource AccentButtonStyle}" Click="AddButton_Click" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="14" />
                    <TextBlock Text="Add recurring" />
                </StackPanel>
            </Button>
        </Grid>

        <InfoBar Grid.Row="1" Margin="0,16,0,0"
                 IsOpen="{x:Bind ViewModel.ErrorMessage, Mode=OneWay, Converter={StaticResource StringNotEmptyToBoolConverter}}"
                 Severity="Error"
                 Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" />

        <Border Grid.Row="2" Margin="0,20,0,0" Style="{StaticResource AppCardStyle}" Padding="20,8">
            <Grid>
                <ListView ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}" SelectionMode="None"
                          Visibility="{x:Bind ViewModel.Items.Count, Mode=OneWay, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=invert}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="entities:RecurringTransaction">
                            <Grid Padding="0,12" ColumnSpacing="14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ContentControl Grid.Column="0" ContentTemplate="{StaticResource CategoryAvatarTemplate}"
                                                 Content="{x:Bind Category}" IsTabStop="False" />
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Category.Name}" FontWeight="SemiBold" />
                                    <TextBlock Text="{x:Bind Wallet.Name}" Style="{StaticResource AppMutedTextStyle}" FontSize="12" />
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="{x:Bind Frequency}" VerticalAlignment="Center"
                                           Style="{StaticResource AppMutedTextStyle}" FontSize="13" Margin="0,0,8,0" />
                                <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center" Margin="0,0,16,0">
                                    <TextBlock Text="Next:" FontSize="13" Opacity="0.6" />
                                    <TextBlock Text="{x:Bind NextDueDate}" FontSize="13" />
                                </StackPanel>
                                <TextBlock Grid.Column="4" Text="{x:Bind Amount, Converter={StaticResource CurrencyConverter}}"
                                           VerticalAlignment="Center" FontWeight="SemiBold"
                                           Foreground="{x:Bind Type, Converter={StaticResource TransactionTypeToBrushConverter}}" Margin="0,0,4,0" />
                                <Button Grid.Column="5" VerticalAlignment="Center" Background="Transparent" BorderThickness="0">
                                    <FontIcon Glyph="&#xE712;" FontSize="16" />
                                    <Button.Flyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem Text="Edit" Icon="Edit" Click="EditButton_Click" Tag="{x:Bind}" />
                                            <ToggleMenuFlyoutItem Text="Active" IsChecked="{x:Bind IsActive}" Click="ToggleActiveButton_Click" Tag="{x:Bind}" />
                                            <MenuFlyoutItem Text="Delete" Icon="Delete" Click="DeleteButton_Click" Tag="{x:Bind}" />
                                        </MenuFlyout>
                                    </Button.Flyout>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Spacing="10" Padding="0,48" HorizontalAlignment="Center"
                            Visibility="{x:Bind ViewModel.Items.Count, Mode=OneWay, Converter={StaticResource CountToVisibilityConverter}}">
                    <FontIcon Glyph="&#xE823;" FontSize="32" Opacity="0.35" HorizontalAlignment="Center" />
                    <TextBlock Text="No recurring transactions yet" FontWeight="SemiBold" HorizontalAlignment="Center" />
                    <TextBlock Text="Add rent, subscriptions, or salary to auto-track them." Style="{StaticResource AppMutedTextStyle}"
                               HorizontalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
