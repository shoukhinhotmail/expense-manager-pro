<Window
    x:Class="ExpenseManager.App.OnboardingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:ExpenseManager.App.Controls">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Border MaxWidth="440" Padding="40" HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="16">
            <StackPanel Spacing="20">
                <Border Width="56" Height="56" CornerRadius="14" Background="{StaticResource AppAccentBrush}"
                        HorizontalAlignment="Left">
                    <FontIcon Glyph="&#xE8C7;" FontSize="26" Foreground="White"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>

                <StackPanel Spacing="4">
                    <TextBlock Text="Welcome to Expense Manager Pro" Style="{StaticResource TitleTextBlockStyle}"
                               TextWrapping="Wrap" />
                    <TextBlock Text="Choose the currency you track money in. You can change this later in Settings."
                               Style="{StaticResource BodyTextBlockStyle}" Opacity="0.7" TextWrapping="Wrap" />
                </StackPanel>

                <controls:CurrencyPickerControl x:Name="Picker" CurrencySelected="Picker_CurrencySelected" />

                <Button x:Name="ContinueButton" Content="Continue" Style="{StaticResource AccentButtonStyle}"
                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                        IsEnabled="False" Click="ContinueButton_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
